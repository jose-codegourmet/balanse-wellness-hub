export const packageDetailPageMeta = {
  purpose: "Public published-package detail with claim or request CTA.",
  whenToUse: "Use on /packages/[slug].",
  whenNotToUse: "Do not use for an owned entitlement ledger.",
} as const;
