/** Public coaches directory contract. */
export const coachesPageMeta = {
  purpose: "Render the searchable public coach directory.",
  whenToUse: "Use for the public coaches route.",
  whenNotToUse: "Do not use for staff or coach administration.",
} as const;
