/** Public class directory contract. */
export const classesPageMeta = {
  purpose: "Display the public, image-led class catalogue.",
  whenToUse: "Use for the public classes route.",
  whenNotToUse: "Do not use for staff class management.",
} as const;
