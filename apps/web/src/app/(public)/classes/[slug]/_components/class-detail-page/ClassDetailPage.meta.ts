/** Public class detail contract. */
export const classDetailPageMeta = {
  purpose: "Show a class story, assigned marketing coaches, gallery, rate, and sessions.",
  whenToUse: "Use for the public class slug route.",
  whenNotToUse: "Do not expose admin coach compensation or editing controls.",
} as const;
