/** Mock contact form contract. */
export const contactFormMeta = {
  purpose: "Validate and display mock contact-message outcomes without sending data.",
  whenToUse: "Use only inside the public contact screen.",
  whenNotToUse: "Do not use for reservations, payments, or real message delivery.",
} as const;
