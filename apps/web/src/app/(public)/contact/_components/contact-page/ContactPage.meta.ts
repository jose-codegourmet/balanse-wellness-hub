/** Public contact screen contract. */
export const contactPageMeta = {
  purpose: "Present studio contact channels, location, social links, and the mock contact form.",
  whenToUse: "Use for the public contact route.",
  whenNotToUse: "Do not use as a booking or payment workflow.",
} as const;
