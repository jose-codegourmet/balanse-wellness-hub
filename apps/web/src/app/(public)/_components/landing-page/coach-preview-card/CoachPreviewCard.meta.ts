/** Compact coach preview used by the public landing screen. */
export const coachPreviewCardMeta = {
  purpose: "Show a public coach photo, specialties, bio, and optional class link.",
  whenToUse: "Use inside public marketing sections that preview coaches.",
  whenNotToUse: "Do not use for admin coach management or private profiles.",
} as const;
