/** Public landing screen contract. */
export const landingPageMeta = {
  purpose: "Marketing homepage with booking hero, class discovery, coaches, and studio story.",
  whenToUse: "Use for the public root route.",
  whenNotToUse: "Do not use for the authenticated portal homepage.",
} as const;
