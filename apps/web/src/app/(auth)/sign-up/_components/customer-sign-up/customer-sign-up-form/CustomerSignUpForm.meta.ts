/** Customer sign-up form contract. */
export const customerSignUpFormMeta = {
  purpose: "Collect mock member profile and password fields for account creation.",
  whenToUse: "Use inside the public customer sign-up screen.",
  whenNotToUse: "Do not use for admin or coach accounts.",
} as const;
