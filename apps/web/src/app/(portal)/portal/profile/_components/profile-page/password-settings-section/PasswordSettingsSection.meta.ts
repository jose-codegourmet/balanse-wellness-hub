/** Password settings contract. */
export const passwordSettingsSectionMeta = {
  purpose: "Validate and display mock password-change outcomes.",
  whenToUse: "Use inside ProfilePage for email-authenticated customers.",
  whenNotToUse: "Do not use as a real credential store.",
} as const;
