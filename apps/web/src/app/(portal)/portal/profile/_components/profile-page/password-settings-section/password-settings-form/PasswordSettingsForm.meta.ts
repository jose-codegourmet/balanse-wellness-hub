/** Password form contract. */
export const passwordSettingsFormMeta = {
  purpose: "Validate and display a mock password update.",
  whenToUse: "Use inside PasswordSettingsSection only.",
  whenNotToUse: "Do not use as a real credential-management form.",
} as const;
