/** Policy history contract. */
export const policyHistorySectionMeta = {
  purpose: "Display the customer’s accepted policy and waiver versions.",
  whenToUse: "Use inside ProfilePage for the policies section.",
  whenNotToUse: "Do not use for admin policy editing.",
} as const;
