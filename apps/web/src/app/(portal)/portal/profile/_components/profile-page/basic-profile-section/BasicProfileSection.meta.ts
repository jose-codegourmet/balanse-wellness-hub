/** Basic profile settings contract. */
export const basicProfileSectionMeta = {
  purpose: "Edit the customer name, email, and contact number in mock mode.",
  whenToUse: "Use inside ProfilePage for the basic section.",
  whenNotToUse: "Do not use for authentication credentials.",
} as const;
