/** Profile section navigation contract. */
export const profileSettingsNavMeta = {
  purpose: "Link the basic, account, password, and policy profile routes.",
  whenToUse: "Use inside the portal profile shell.",
  whenNotToUse: "Do not use for global portal navigation.",
} as const;
