/** Basic profile form contract. */
export const basicProfileFormMeta = {
  purpose: "Validate and save customer identity and contact fields in mock mode.",
  whenToUse: "Use inside BasicProfileSection only.",
  whenNotToUse: "Do not embed this form in the profile page shell.",
} as const;
