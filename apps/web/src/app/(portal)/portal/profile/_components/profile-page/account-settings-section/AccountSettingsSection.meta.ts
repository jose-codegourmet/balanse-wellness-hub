/** Connected account settings contract. */
export const accountSettingsSectionMeta = {
  purpose: "Show the selected customer sign-in provider and mock management affordance.",
  whenToUse: "Use inside ProfilePage for the account section.",
  whenNotToUse: "Do not use as a real OAuth linking workflow.",
} as const;
