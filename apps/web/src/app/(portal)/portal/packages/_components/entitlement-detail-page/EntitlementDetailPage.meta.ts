export const entitlementDetailPageMeta = {
  purpose: "Owned package ledger: remaining sessions and redemption history.",
  whenToUse: "Use on /portal/packages/[entitlementId].",
  whenNotToUse: "Do not use for public catalogue detail.",
} as const;
