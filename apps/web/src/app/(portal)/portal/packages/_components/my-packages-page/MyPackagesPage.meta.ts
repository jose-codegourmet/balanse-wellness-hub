export const myPackagesPageMeta = {
  purpose: "Customer list of owned packages and pending requests.",
  whenToUse: "Use on /portal/packages.",
  whenNotToUse: "Do not use for the public catalogue.",
} as const;
