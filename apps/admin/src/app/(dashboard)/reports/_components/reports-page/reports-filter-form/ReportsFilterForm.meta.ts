/** Reports filter form contract. */
export const reportsFilterFormMeta = {
  purpose: "Filter report tables by class, coach, and session status.",
  whenToUse: "Use inside the reports screen.",
  whenNotToUse: "Do not use as a general dashboard filter bar.",
} as const;
