export const roleListPageMeta = {
  purpose: "Catalogue of built-in and custom staff roles with assignment and permission counts.",
  whenToUse: "Use on /staff/roles.",
  whenNotToUse: "Do not hide this route from the sidebar here — #295 owns nav gating.",
} as const;
