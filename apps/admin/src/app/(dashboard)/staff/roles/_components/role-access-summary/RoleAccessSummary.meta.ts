export const roleAccessSummaryMeta = {
  purpose: "Live preview of admin pages and sensitive data a permission checklist would unlock.",
  whenToUse: "Show beside or below the role checklist while creating or editing a role.",
  whenNotToUse: "Do not treat this as authorization. Server/mock checks remain canonical.",
} as const;
