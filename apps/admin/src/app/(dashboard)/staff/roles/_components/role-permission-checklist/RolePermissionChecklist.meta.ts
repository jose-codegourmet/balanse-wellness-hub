export const rolePermissionChecklistMeta = {
  purpose:
    "Grouped permission checklist generated from PERMISSION_REGISTRY, with select/clear group.",
  whenToUse: "Use inside RoleForm. Import keys from @balanse/domain — never copy the list.",
  whenNotToUse: "Do not use as a standalone form or invent permission strings.",
} as const;
