export const staffRoleWarningsMeta = {
  purpose: "Coach-link, last Super Admin, and self-lockout warnings for staff role assignment.",
  whenToUse: "Use on the staff form next to Role and isCoach.",
  whenNotToUse: "Do not treat these as a substitute for adapter/domain invariants.",
} as const;
