export const staffRoleSummaryMeta = {
  purpose: "Summarize the assigned authorization role, pages, and sensitive data.",
  whenToUse: "Use on staff create/edit below the role picker.",
  whenNotToUse: "Do not infer this from isCoach or a Coach profile.",
} as const;
