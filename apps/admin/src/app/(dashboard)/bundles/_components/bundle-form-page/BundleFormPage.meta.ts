export const bundleFormPageMeta = {
  purpose: "Create or edit a session package definition.",
  whenToUse: "Use on /bundles/new and /bundles/[bundleId].",
  whenNotToUse: "Do not use for granting a package to one customer.",
} as const;
