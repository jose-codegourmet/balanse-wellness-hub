export const bundleListPageMeta = {
  purpose: "Admin catalogue of session packages (bundles) with status and review queue.",
  whenToUse: "Use on /bundles.",
  whenNotToUse: "Do not use for customer package browsing.",
} as const;
