# Admin notification header

The header is the single operational attention surface for waitlisted bookings, payment review, cancellation requests, and reschedule requests. It reads the dashboard snapshot and links each notification to its queue. The sidebar remains destination-only, so counts do not repeat across the shell.
