# Session editor

RHF/Zod schedule wizard. A session belongs to a class and independently requires one or more coaches. Optional session title is trimmed; blank titles fall back to the current class name on all session displays. Class changes supply price and duration defaults, not a mandatory coach assignment. Coach compensation snapshots are admin-only and retained per assignment.
