/**
 * FE foundation does not apply hosted migrations.
 * INF-003 owns the first real migration against the shared project.
 */
console.log("Skipping Supabase auth preflight (FE foundation placeholder schema).");
