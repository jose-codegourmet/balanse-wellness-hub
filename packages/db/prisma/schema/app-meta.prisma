/// INF-003 baseline. Business tables land in BE-001+ as sibling schema files.
model AppMeta {
  id        String   @id @default(cuid())
  key       String   @unique
  value     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([key])
  @@map("app_meta")
}
