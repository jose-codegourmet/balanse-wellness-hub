# asset-manifest.ts follow-up (ASSET-015)

After merging binaries, add `"coaches-b"` to `MARKETING_SLOTS_WITH_THUMBS` in
`packages/domain/src/asset-manifest.ts`, and update the specialty-accent comment
to note that coaches-b shipped 2026-09-20 for the 8-coach cast (Alec/Sofia/Kate
still excluded).

The JSON manifest (`packages/domain/src/asset-manifest.json` + `docs/assets/manifest.json`)
already marks coaches-b approved with working paths.
